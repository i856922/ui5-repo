{
	"role":{
		"name": "chickenwingdemo.db.roles::chicken_wings",
		"schema_privileges": [{
		   "privileges": ["SELECT METADATA",
		    			  "SELECT CDS METADATA",
		    			  "SELECT", "UNMASKED"
		    			  
		   ]
		}]
		

      }
}
